import de from "./de";
import en from "./en";
import es from "./es";
import it from "./it";

export const translations = { en, it, de, es };
export const SUPPORTED_LANGUAGES = Object.freeze(["it", "en", "de", "es"]);
export const DEFAULT_LANGUAGE = "en";
